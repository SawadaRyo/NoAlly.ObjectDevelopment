//日本語コメント可
using UnityEngine;
using UniRx;
using UniRx.Triggers;

public enum PlayerActionState
{
    None,
    Move,
    Jump,
    Attack,
    Attacking,
    Damaged,
    Damaging
}


public class InputStateMachine : MonoBehaviour
{
    void Start()
    {
         
    }
    void UpdateToInput()
    {
        Observable.
    }
}
